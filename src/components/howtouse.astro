---
import { Picture } from "@astrojs/image/components";
import showImage from "assets/show.png";
---

<section class="mt-20 rounded-3xl border border-[var(--line-soft)] bg-white p-6 sm:p-8 lg:p-10 shadow-sm">
  <div class="grid lg:grid-cols-2 gap-8 items-start">
    <div>
      <p class="text-xs uppercase tracking-[0.18em] text-pink-600 font-semibold">How It Works</p>
      <h2 class="mt-3 text-3xl lg:text-4xl font-bold tracking-tight text-gray-900">Get Started in 3 Steps</h2>
      <p class="mt-4 text-gray-600">Set up your AI-powered WhatsApp assistant in minutes — no code required.</p>

      <div class="mt-7 space-y-4">
        <div class="rounded-2xl border border-[var(--line-soft)] p-4 bg-[var(--brand-50)]">
          <p class="text-sm font-semibold text-pink-700">Step 1</p>
          <h3 class="font-semibold text-gray-900 mt-1">Install and connect your AI model</h3>
          <p class="text-gray-600 mt-1">Add the extension from Chrome Web Store, then connect OpenAI, OpenRouter, Groq, or your local model.</p>
        </div>

        <div class="rounded-2xl border border-[var(--line-soft)] p-4 bg-white">
          <p class="text-sm font-semibold text-pink-700">Step 2</p>
          <h3 class="font-semibold text-gray-900 mt-1">Set up your Persona</h3>
          <p class="text-gray-600 mt-1">Define tone, style, and boundaries so every reply feels natural and on-brand.</p>
        </div>

        <div class="rounded-2xl border border-[var(--line-soft)] p-4 bg-white">
          <p class="text-sm font-semibold text-pink-700">Step 3</p>
          <h3 class="font-semibold text-gray-900 mt-1">Enable automation and manage leads</h3>
          <p class="text-gray-600 mt-1">Turn on auto-reply rules, manage leads from the Toolbox, and monitor everything in the Dashboard.</p>
        </div>
      </div>
    </div>

    <div class="relative flex items-center justify-center min-h-[420px]">
      <div class="relative w-full max-w-md">
        <Picture
          src={showImage}
          alt="Agent for WhatsApp dashboard preview"
          widths={[200, 300, 500, 800]}
          sizes="(max-width: 1024px) 100vw, 420px"
          loading="eager"
          format="avif"
          class="rounded-2xl w-full opacity-75"
        />
        <div class="absolute inset-0 rounded-2xl bg-gradient-to-b from-white/12 via-white/28 to-white/42"></div>

        <a
          href="https://chromewebstore.google.com/detail/agent-for-whatsapp/kjeodciaphncjcogidpafdeeckmpdcpn"
          data-cta="howto_install"
          data-location="howto"
          target="_blank"
          rel="noopener"
          class="absolute inset-0 m-auto h-fit w-fit inline-flex items-center justify-center rounded-xl bg-rose-600/95 hover:bg-rose-700 text-white font-semibold px-5 py-3 shadow-lg shadow-rose-500/35"
        >
          Install Free
        </a>
      </div>
    </div>
  </div>
</section>
