---
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon";
---

<section class="grid lg:grid-cols-2 gap-8 items-center pt-10 pb-6 md:pt-14 md:pb-10">
  <div>
    <p class="inline-flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.18em] text-pink-600 bg-[var(--brand-50)] border border-[var(--line-soft)] rounded-full px-3 py-1">
      <span class="w-2 h-2 rounded-full bg-green-500"></span>
      Chrome Extension for WhatsApp Web
    </p>

    <div class="mt-5 text-4xl sm:text-5xl lg:text-6xl font-bold leading-tight tracking-tight text-gray-900">
      <span class="block">Automate Replies</span>
      <span class="inline-block mt-1 text-gray-700">with Agent for </span>
      <span class="inline-block mt-1 relative min-w-[180px]">
        <span id="dynamic-text-0" class="platform-name visible">WhatsApp</span>
        <span id="dynamic-text-1" class="platform-name">Support</span>
        <span id="dynamic-text-2" class="platform-name">Sales</span>
        <span id="dynamic-text-3" class="platform-name">Leads</span>
      </span>
    </div>

    <p class="text-lg mt-6 text-gray-700 max-w-2xl">
      Connect your preferred AI model, customize reply behavior with persona settings,
      and control everything from a simple dashboard. Includes lead management,
      message workflows, and usage insights.
    </p>

    <div class="mt-8 flex flex-col sm:flex-row gap-3">
      <Link
        href="https://chromewebstore.google.com/detail/agent-for-whatsapp/kjeodciaphncjcogidpafdeeckmpdcpn"
        data-cta="hero_add_to_chrome"
        data-location="hero"
        target="_blank"
        class="flex gap-2 items-center justify-center bg-pink-600 hover:bg-pink-700 rounded-xl text-white font-semibold"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="bx:bxl-chrome" />
        Add to Chrome — Free
      </Link>

      <Link
        size="lg"
        rel="noopener"
        href="/tools/whatsapp-reply-generator"
        data-cta="hero_try_free_tool"
        data-location="hero"
        class="flex gap-2 items-center justify-center bg-white border border-[var(--line-soft)] text-gray-900 rounded-xl hover:bg-[var(--brand-50)] font-semibold">
        <Icon class="text-pink-600 w-5 h-5" name="bx:bxs-magic-wand" />
        Try Free Tool
      </Link>

      <Link
        size="lg"
        rel="noopener"
        href="https://aow.me/try?utm_source=site&utm_medium=hero&utm_campaign=live_demo"
        data-cta="hero_live_demo"
        data-location="hero"
        class="flex gap-2 items-center justify-center bg-green-50 text-gray-900 rounded-xl hover:bg-green-100 border border-green-200 font-semibold"
        target="_blank">
        Live Demo
      </Link>
    </div>

    <div class="mt-6 flex flex-wrap gap-2 text-sm text-gray-700">
      <span class="px-3 py-1 rounded-full border border-[var(--line-soft)] bg-white">OpenAI · OpenRouter · Groq</span>
      <span class="px-3 py-1 rounded-full border border-[var(--line-soft)] bg-white">Persona presets</span>
      <span class="px-3 py-1 rounded-full border border-[var(--line-soft)] bg-white">Lead management</span>
    </div>
  </div>

  <div class="bg-white border border-[var(--line-soft)] rounded-2xl shadow-sm p-5 sm:p-6">
    <p class="text-xs uppercase tracking-[0.16em] text-pink-600 font-semibold">Why teams switch</p>
    <ul class="mt-4 space-y-3">
      <li class="flex items-start gap-3">
        <Icon name="mdi:check-circle" class="w-5 h-5 text-green-500 mt-0.5" />
        <div>
          <p class="font-semibold text-gray-900">Bring your own AI model</p>
          <p class="text-sm text-gray-600">Switch between OpenAI, OpenRouter, Groq, or local models to balance quality, speed, and cost.</p>
        </div>
      </li>
      <li class="flex items-start gap-3">
        <Icon name="mdi:check-circle" class="w-5 h-5 text-green-500 mt-0.5" />
        <div>
          <p class="font-semibold text-gray-900">Natural, on-brand replies</p>
          <p class="text-sm text-gray-600">Define persona tone and style so every response feels consistent and under your control.</p>
        </div>
      </li>
      <li class="flex items-start gap-3">
        <Icon name="mdi:check-circle" class="w-5 h-5 text-green-500 mt-0.5" />
        <div>
          <p class="font-semibold text-gray-900">Built-in lead & workflow tools</p>
          <p class="text-sm text-gray-600">Manage leads, automate message workflows, and track usage — all from one dashboard.</p>
        </div>
      </li>
    </ul>

    <div class="mt-6 grid grid-cols-3 gap-3">
      <div class="rounded-xl bg-[var(--brand-50)] border border-[var(--line-soft)] p-3 text-center">
        <p class="text-xl font-bold text-pink-600">4+</p>
        <p class="text-xs text-gray-600">AI providers</p>
      </div>
      <div class="rounded-xl bg-[var(--brand-50)] border border-[var(--line-soft)] p-3 text-center">
        <p class="text-xl font-bold text-pink-600">5m</p>
        <p class="text-xs text-gray-600">quick setup</p>
      </div>
      <div class="rounded-xl bg-[var(--brand-50)] border border-[var(--line-soft)] p-3 text-center">
        <p class="text-xl font-bold text-pink-600">24/7</p>
        <p class="text-xs text-gray-600">auto-reply</p>
      </div>
    </div>
  </div>
</section>

<style>
  .platform-name {
    position: absolute;
    opacity: 0;
    transition: opacity 0.4s ease;
    color: #e13ca5;
  }

  .platform-name.visible {
    opacity: 1;
    position: relative;
  }
</style>

<script>
  let currentIndex = 0;
  const words = ["WhatsApp", "Support", "Sales", "Leads"];

  setInterval(() => {
    const currentEl = document.getElementById(`dynamic-text-${currentIndex}`);
    if (currentEl) currentEl.classList.remove("visible");

    currentIndex = (currentIndex + 1) % words.length;

    const nextEl = document.getElementById(`dynamic-text-${currentIndex}`);
    if (nextEl) nextEl.classList.add("visible");
  }, 2200);
</script>
